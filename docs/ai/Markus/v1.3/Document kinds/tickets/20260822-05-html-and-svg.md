---
id: 20260822-05-html-and-svg
title: HTML and SVG
type: feature
priority: high
status: in-progress
created: 2026-08-22
updated: 2026-08-22
closed:
notes: 'model_tier: premium'
parent:
depends_on:
- 20260822-02-syntaxprofile-and-fold-generalization
subtasks: []
plan_status: done
---
## Description

Shared XML/HTML tokenizer for HTML and SVG. Fold elements with matching end tags; void/self-closing not foldable. Source = 1:1 + folds. Preview = locked WKWebView of the buffer (no script, no network). Switching modes must not unmount the document session (v1.1 Settings lesson). HTML/SVG open in Source.

## Acceptance criteria

- [x] `.html` / `.svg` fold elements in Source (R6).
- [x] Preview is script-free, no-network WebView of the buffer (R7, N5).
- [x] Source/Preview switch keeps the session and folds (R7).
- [x] macOS + iOS/iPad Debug builds (N3).

## Context

Depends on 02. R6, R7, N5. Not Markdown `PreviewSubstitution`.

NO TDD. Verify by build.

## Subtasks

- [ ] Shared HTML/XML folds + outline + diagnostics.
- [ ] Locked WKWebView Preview.
- [ ] Session stays mounted across mode switch.

## Implementation plan

Status: done
Current task: 

### T01: Shared HTML/XML tokenizer → element folds + outline + diagnostics + budget

Byte-offset HTML/XML tokenizer (one engine). Walk UTF-8, track 1-based lines the same way `JSONScanner` / `SourceMap` do (`\n` only). `HTMLScanDialect.html` is case-insensitive with HTML5 void + rawtext (`script`/`style`/…). `HTMLScanDialect.xml` is case-sensitive, no HTML void list, understands CDATA (SVG). Fold an element only when a matching end tag lands on a later line than the opener. Void tags and `/>` self-closing tags are not foldable. `Block.kind` is `MarkdownBlockKind.other`. `FoldID.Kind` gains `element`. Fold extent is `openerEnd..<closerEnd` (opener line stays visible, same as fences/JSON). Outline: tag name plus `#id` / first `.class` when those attributes are cheap to read. Diagnostics for unclosed/malformed/mismatched tags (capped). `HTMLScanBudget` mirrors `JSONScanBudget` (max bytes, foldables, outline, spans, 50 ms). Over budget: stop, keep partial folds, warning diagnostic.

Files: `Markus/Markus/Syntax/HTMLScanner.swift`, `Markus/Markus/Markdown/BlockIndex.swift`

Verify:
```
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=macOS' -configuration Debug build
```
- [ ] todo
- [x] done
### T02: HTMLSyntaxProfile / SVG factory wiring

`HTMLSyntaxProfile` wraps the shared scanner. `analyze` uses `HTMLScanBudget.default`. Factory: `html` dialect for `.html`, `xml` dialect for `.svg` — not a second engine. `SyntaxProfiles.profile(for:)` returns that profile for both kinds. Empty buffer does not crash.

Files: `Markus/Markus/Syntax/HTMLSyntaxProfile.swift`, `Markus/Markus/Syntax/SyntaxProfile.swift`

Verify:
```
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=macOS' -configuration Debug build
```
- [ ] todo
- [x] done
### T03: Locked WKWebView Preview overlay; session stays mounted

`ContentView.editorColumn` keeps `SessionEditorRepresentable(session:)` in the hierarchy for every kind. When kind is html/svg, overlay a locked `WKWebView` (hidden in Source, visible in Preview via opacity / `allowsHitTesting`). Do not `if preview { WebView } else { SessionEditorRepresentable }`. Load with `loadHTMLString(_:baseURL: nil)` (SVG wrapped in a minimal HTML shell). `allowsContentJavaScript = false`. Navigation delegate cancels `http`/`https`/`file` and link-activated navigations. Non-persistent data store. Treat the buffer as untrusted (N5). `FoldingSession.rebuildSubstitutionIndex` stays Markdown-only so HTML Preview is not GFM substitution. HTML/SVG already open in Source via `applyKind`.

Files: `Markus/Markus/Preview/LockedHTMLPreview.swift`, `Markus/Markus/ContentView.swift`, `Markus/Markus/Editor/FoldingTextView.swift`

Verify:
```
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=macOS' -configuration Debug build
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=iOS Simulator,name=iPhone 17' -configuration Debug build
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5)' -configuration Debug build
```
- [ ] todo
- [x] done
### T04: Refresh WebView from buffer without unmounting; iOS+Mac

The html/svg WebView stays mounted across Source/Preview (only opacity/hit-testing change). `updateNSView` / `updateUIView` reloads from the current buffer string without recreating the `WKWebView` or the FoldingTextView. Debounce reloads (~120 ms, same spirit as editor reparse) so typing does not thrash; do not drop the document session. Same lockdown on iOS and Mac.

Files: `Markus/Markus/Preview/LockedHTMLPreview.swift`, `Markus/Markus/ContentView.swift`

Verify:
```
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=macOS' -configuration Debug build
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=iOS Simulator,name=iPhone 17' -configuration Debug build
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5)' -configuration Debug build
```
- [ ] todo
- [x] done

### T05: Install content blockers before any buffer load

N5: compile (or look up) the WK content-rule list and add it to the **same** `WKUserContentController` instance used to construct the `WKWebView`, before any `loadHTMLString`. Do not add to `webView.configuration` (a copy). Do not load the buffer until rules are installed. Do not load while Source (`isVisible == false`). Block all subresource URL types with a blanket `url-filter` of `.*` (not only `https?` and `file`). If compile fails, fail closed: never load the untrusted buffer.

Files: `Markus/Markus/Preview/LockedHTMLPreview.swift`

Verify:
```
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=macOS' -configuration Debug build
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=iOS Simulator,name=iPhone 17' -configuration Debug build
xcodebuild -project Markus.xcodeproj -scheme Markus -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5)' -configuration Debug build
```
- [x] done

## Notes

Append-only running log. Each entry dated.

### 2026-08-22
Wrote implementation plan T01–T04 (shared tokenizer+budget; HTMLSyntaxProfile+SVG factory; locked WKWebView overlay with session mounted; debounce refresh). NO TDD. Verify by xcodebuild build.

### 2026-08-22
T01: HTMLScanner walks UTF-8 offsets; FoldID.Kind element; fold extent after opener line; MarkdownBlockKind.other; void/self-closing not foldable; HTMLScanBudget. macOS Debug BUILD SUCCEEDED. Did not run xcodebuild test.

### 2026-08-22
T02: HTMLSyntaxProfile wraps the shared scanner; profile(for: .html) uses HTML dialect, .svg uses XML dialect. macOS Debug BUILD SUCCEEDED.

### 2026-08-22
T03: Locked WKWebView overlay; SessionEditorRepresentable stays mounted; JS off; http/https/file cancelled; loadHTMLString baseURL nil; Markdown substitution stays markdown-only. macOS + iPhone 17 + iPad Pro 13-inch (M5) Debug BUILD SUCCEEDED.

### 2026-08-22
T04: html/svg WKWebView stays mounted across Source/Preview (opacity/hit-testing only); buffer reloads debounce 120 ms without recreating the session or WebView. macOS + iPhone 17 + iPad Pro 13-inch (M5) Debug BUILD SUCCEEDED. Ticket left in-progress (no visual open of a .html/.svg file; no xcodebuild test).

## Review

2026-08-22 — **Important**. Do not mark `done`.

Commits: `6b9c489` T01, `bd0cc80` T02, `50f2b1f` T03, `9f029c7` T04.

- **Important (N5):** Subresource network/`file://` loads are not actually locked before (and possibly after) first `loadHTMLString`. `installNetworkBlocker` compiles rules asynchronously after `WKWebView` init, then calls `webView.configuration.userContentController.add` (configuration is a copy). `makeNSView`/`makeUIView` load immediately. Navigation delegate does not see `<img>`/`<link>`/`<base href>`. T04 keeps this WebView mounted in Source, so opening a `.html` can fetch before Preview. `LockedHTMLPreview.swift:51`, `:73`, `:100`, `:191-201`.
- **Minor:** `allows` permits `data:` (`LockedHTMLPreview.swift:31`).
- **Minor:** SVG wrapper interpolates the raw buffer (`LockedHTMLPreview.swift:20`).
- **Minor:** Content-rule compile failure is ignored (`LockedHTMLPreview.swift:198`).
- **Minor:** No on-screen open of a `.html`/`.svg` file (commit criteria).

R6 (shared tokenizer), R7 session mount (`ContentView.swift:115-131`), and Markdown substitution (`FoldingTextView.swift:831-843`, `reparse` markdown-only) look correct. Controller must not mark `done` until the N5 finding is fixed.

### 2026-08-22
Debug: N5 fail was async content-rule compile after WKWebView init, then add on `webView.configuration` (a copy), plus immediate `loadHTMLString` while still in Source. T05 compiles/looks up rules, adds them to the constructing `WKUserContentController`, loads only when rules are ready and Preview is visible, fail-closed if compile fails.

## Review (T05)

2026-08-22 — **Minor.** Controller may mark done.

`20260822-05 T05` installs content blockers on the constructing `WKUserContentController` before any `loadHTMLString`, loads only in Preview after rules are ready, fail-closed if compile fails. Script remains off; main-frame http/https/file still cancelled. Remaining items stay Minor (`data:` allowed, SVG wrapper interpolates buffer).
