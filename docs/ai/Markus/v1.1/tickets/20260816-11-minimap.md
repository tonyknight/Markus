---
id: 20260816-11-minimap
title: "Minimap"
type: feature
priority: medium
status: todo
created: 2026-08-16
updated: 2026-08-16
closed:
notes: ""
parent:
depends_on:
  - 20260816-04-fold-all-unfold-all-and-fence-placeholder
subtasks:
  - id: T1
    title: Structural rendering (not uniform grey bars)
    status: todo
  - id: T2
    title: Viewport indicator overlay
    status: todo
  - id: T3
    title: Click-to-scroll via existing sourceLine(atViewY:)
    status: todo
  - id: T4
    title: Fold-aware rendering; downsample at 5 MB scale
    status: todo
  - id: T5
    title: Refresh on document change, not just on SwiftUI re-render
    status: todo
---

## Description

Today's minimap draws uniform grey bars, one per source line, with no
token colour, no viewport indicator, and no click-to-scroll — the
`sourceLine(atViewY:)` API exists but nothing calls it — and it
refreshes only when SwiftUI happens to re-render. This ticket improves
it for macOS: structural rendering, a viewport indicator, click-to-scroll,
fold-awareness, and downsampling at the 5 MB target.

## Acceptance criteria

- [ ] The minimap reflects document structure rather than uniform bars
      (R18).
- [ ] It indicates the current viewport (R18).
- [ ] Clicking scrolls to the corresponding location, using the existing
      `sourceLine(atViewY:)` API (R18).
- [ ] It stays fold-aware (folded regions do not show as if expanded)
      (R18).
- [ ] At the 5 MB target it downsamples rather than drawing one bar per
      line (G.19).
- [ ] It refreshes when the document changes, not only when SwiftUI
      happens to re-render (G.19).

## Context

- Requirements: R18; Architecture component 11 "Minimap (macOS)".
- Planning doc `(2026-08-16) v1.1.md`: G.19.
- Depends on ticket 04 for fold-awareness (needs a working fold service
  to know what's currently folded).
- Testing requirements: minimap is explicitly named as macOS-only chrome
  — macOS destination only.

## Subtasks

- [ ] Render structure (e.g. heading/token density) instead of uniform
      grey bars.
- [ ] Add a viewport indicator overlay.
- [ ] Wire click events to `sourceLine(atViewY:)` for scroll-to-click.
- [ ] Make rendering fold-aware; skip/compress folded regions.
- [ ] Implement downsampling for large documents (5 MB target).
- [ ] Hook refresh to document-change events rather than relying on
      incidental SwiftUI re-renders.

## Implementation plan

Status: draft
Current task:

## Notes

Append-only running log. Each entry dated.
