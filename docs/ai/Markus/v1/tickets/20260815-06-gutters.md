---
id: 20260815-06-gutters
title: Gutters
type: feature
priority: medium
status: done
created: 2026-08-15
updated: 2026-08-16
closed: 2026-08-16
notes: T04 review minors-only; marking done
parent:
depends_on:
- 20260815-05-mode-chrome
subtasks:
- id: S1
  title: Source line map
  status: done
- id: S2
  title: Mac line-number fold rail both modes
  status: done
- id: S3
  title: iOS optional numbers + slim rail
  status: done
- id: S4
  title: Three-destination verify
  status: done
plan_status: done
---
## Description

Mac line numbers + fold chevrons in both modes. iOS optional numbers +
slim fold rail. Source line map, go-to-line ready.

## Acceptance criteria

- [x] Mac Preview and Source show source line numbers and fold chevrons
- [x] iPhone/iPad can hide line numbers and still fold from a slim gutter
- [x] Numbers are source lines, not visual wrapped lines
- [x] Line map is usable for later go-to-line
- [x] Tests pass on Mac, iPhone simulator, and iPad simulator

## Context

Requirements R6, R7.

## Subtasks

- [x] Mac line-number fold rail both modes
- [x] iOS optional numbers + slim rail
- [x] Source line map
- [x] Three-destination verify

## Implementation plan

Status: done
Current task: 

### T01: Source line map
Build a **source-line map** from TextKit 2 fragments (packed Y, same as fold hide): each **source** line (1-based, newline-split of the full buffer) maps to a visible Y or is omitted when that line sits inside a folded extent. Wrapped visual lines still share one source number (R6/R7: numbers are source lines, not wrap rows). API must be go-to-line ready: `y(forSourceLine:)` and `sourceLine(atY:)` (or equivalent). Tests: fixture with a heading + wrapped paragraph + fence; line 1 Y < later visible lines; a folded heading body is not a visible source line; wrap does not invent extra numbers. Do not implement go-to-line UI (ticket 10). Do not squash paragraph styles.
Files: `Markus/Markus/Editor/` (line map on `FoldingSession`/`FoldingTextView`), `Markus/MarkusTests/` 
Verify: `xcodebuild -project Markus/Markus.xcodeproj -scheme Markus -destination 'platform=macOS' -only-testing:MarkusTests test`
- [ ] todo
- [x] done
### T02: Mac line-number fold rail
On **macOS**, both Preview and Source show a gutter with **source line numbers** and **fold chevrons** on foldable heading/fence start lines. Chevron toggle uses the existing `FoldStore` (shared across mode). Inset the text container so numbers are not painted over glyphs. Mac `showLineNumbers` is always on. Tests: numbers present in both modes; chevron on a heading toggles fold and `collapsedFragmentCount`; save still full UTF-8. No minimap, no tabs.
Files: `Markus/Markus/Editor/FoldingTextView.swift` (and gutter view if split out), tests
Verify: `xcodebuild -project Markus/Markus.xcodeproj -scheme Markus -destination 'platform=macOS' -only-testing:MarkusTests test`
- [ ] todo
- [x] done
### T03: iOS optional numbers + slim fold rail
On **iPhone/iPad**, line numbers are **optional** (`showLineNumbers` user flag; default may be on). When numbers are off, a **slim fold rail** remains and chevrons still toggle folds in Preview and Source. Tests: hide numbers → width shrinks or numbers are absent, chevron still folds; show numbers → source lines appear. Do not add Mac-only always-on behavior on iOS.
Files: `FoldingTextView` / session `showLineNumbers`, tests
Verify: `xcodebuild -project Markus/Markus.xcodeproj -scheme Markus -destination 'platform=macOS' -only-testing:MarkusTests test`
- [ ] todo
- [x] done
### T04: Three-destination verify
Shared editor/gutter/fold code must pass all three destinations.
Verify:
`xcodebuild -project Markus/Markus.xcodeproj -scheme Markus -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MarkusTests test`
and
`xcodebuild -project Markus/Markus.xcodeproj -scheme Markus -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5)' -only-testing:MarkusTests test`
Files: tests as needed; no folder tree, no theme picker, no WebKit
- [ ] todo
- [x] done

## Notes

### 2026-08-16
T01 done: packed source-line map (y/forSourceLine, sourceLine/atY); folded extents omitted; wrap shares one number. SHA 891e30d

### 2026-08-16
T02 done: Mac gutter source numbers + chevrons both modes, text inset, FoldStore toggle, save full UTF-8. SHA a59ec51

### 2026-08-16
T03 done: iOS showLineNumbers optional; slim chevron rail when off; Mac still always-on. SHA 80ac706

### 2026-08-16
T04 done: MarkusTests TEST SUCCEEDED on iPhone 17 and iPad Pro 13-inch (M5). Ticket left in-progress; no T04 code commit.

## Review

- **Date:** 2026-08-16
- **Verdict:** Minor only — done
- **Verify (controller, fresh):** MarkusTests TEST SUCCEEDED on macOS, iPhone 17, iPad Pro 13-inch (M5).
- **Findings:**
  1. **Minor:** iOS `showLineNumbers` is API-only; no ContentView/app-storage toggle yet.
  2. **Minor:** Chevron hit-testing is untested (`toggleFold(atSourceLine:)` is used instead).
  3. **Minor:** T01 is tests+code in one commit (no RED SHA).
  4. **Minor:** iOS `didSet` does not mark the view dirty; SwiftUI `ensureLayout()` covers it.
